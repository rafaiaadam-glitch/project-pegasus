# Export

- Produce PDF, Markdown, and Anki-compatible CSV outputs.

Implementation note:
- `pipeline/run_pipeline.py --export` writes Markdown, PDF, and Anki CSV to
  `storage/exports/<lecture-id>`.
