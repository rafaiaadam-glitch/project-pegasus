# Generation

- Generate structured artifacts per preset.
- Target schemas in `/schemas/artifacts/`.

Implementation note:
- `pipeline/run_pipeline.py --use-llm` uses OpenAI responses to populate
  schema-compliant artifacts with real content.
