{
  "name": "project-pegasus",
  "private": true,
  "type": "module",
  "scripts": {
    "test:core": "tsx --test core/thread_engine/*.test.ts",
    "test:e2e": "playwright test",
    "e2e:install-deps": "playwright install --with-deps chromium",
    "test:e2e:ci": "npm run e2e:install-deps && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.5.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "react-native-gesture-handler": "^2.30.0"
  }
}
